<h2 class="login">Sign up</h2>
<div class="login-main-div">
  <div class="form-inputs">
    <%= simple_form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
      <%= f.error_notification %>
      <div class="form">
        <%= f.input :first_name,
                    required: true,
                    autofocus: true,
                    input_html: { autocomplete: "first-name", class: "emailpassword" } %>
        <%= f.input :last_name,
                    required: true,
                    autofocus: true,
                    input_html: { autocomplete: "last-name", class: "emailpassword" } %>
        <%= f.input :age,
                    required: true,
                    autofocus: true,
                    input_html: { autocomplete: "age", class: "emailpassword" } %>
        <%= f.input :email,
                    required: true,
                    autofocus: true,
                    input_html: { autocomplete: "email", class: "emailpassword" } %>
        <%= f.input :password,
                    required: true,
                    hint: ("#{@minimum_password_length} characters minimum" if @minimum_password_length),
                    input_html: { autocomplete: "new-password", class: "emailpassword" } %>
        <%= f.input :password_confirmation,
                    required: true,
                    input_html: { autocomplete: "new-password", class: "emailpassword" } %>
        <%= f.input :description,
                    required: true,
                    autofocus: true,
                    input_html: { autocomplete: "description", class: "emailpassword" } %>
        <div class="form-actions-2">
          <%= f.button :submit, "Sign up", class: "login-boton-2" %>
          <div class="render">
            <%= render "devise/shared/links" %>
          </div>
        </div>
      </div>
    </div>
  </div>
<% end %>
